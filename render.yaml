services:
  - type: web
    name: turfa-telegram-bot
    runtime: php
    buildCommand: "composer install --no-dev 2>/dev/null || true"
    startCommand: "php -S 0.0.0.0:$PORT"
    envVars:
      - key: BOT_TOKEN
        sync: false
